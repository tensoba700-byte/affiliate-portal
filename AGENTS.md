# 🤖 Antigravity 記事執筆・開発運用ルール (永続記憶ドキュメント)

太郎さんとのこれまでの対話から得た教訓、アフィリエイトリンクの取得ルール、ペルソナ、および開発運用における絶対遵守事項です。**次回以降、新しいセッション（新しいページ）を開始した際は、何よりも先にこのファイルを必ず読み込み、すべての指示を完全に再現すること。**

---

## 1. 太郎さんとの対話・フローの約束 (認証排除)
* **事前確認・承認ボタンの完全排除：**
  - 太郎さんから「記事を書いて」と指示された際、事前の構成案の提示、プラン（Implementation Plan）の作成、および「承認ボタン（認証）」を求めるプロセスは**一切不要（完全省略）**とする。
  - 指示を受けたら即座に、直接記事の執筆と画像生成、GitHubへのプッシュ（非公開ストック保存）までを私の判断で一気に完了させること。
  - Notionへのデータ登録や更新などの定型作業についても、事前の確認や承認を得るプロセスは一切不要とし、指示を受けたら即座に私の判断で実行を完了させること。
  - 途中経過の逐一報告は不要とし、すべての作業が完了した後に結果のみを簡潔に伝えること。
* **上書きクリーンアップの徹底 (古いゴミの完全排除)：**

  - 記事やプログラムの修正が完了し「OK」になったら、古いテストデータや古い蓄積データ（`stockpile_data.json` など）は必ず最新の正しい状態で完全に上書きし、古いゴミを残さない。
  - AIが古いファイルや過去のキャッシュデータを誤読して同じバグを繰り返す悪癖を防止するため、不要になったスクラッチファイルは即座に削除（クリーンアップ）すること。常に最新のファイルのみを唯一のマスターデータとして扱う。

---

## 2. アフィリエイトリンクの絶対取得ルール (直リンク優先)
独自に検索APIやクエリを使って「検索・逆引き」しようとすると、エラーやリンク切れ、誤認が多発するため、以下のフローを厳守する。

* ** mybest等の競合サイト上の直リンクを最優先で取得すること：**
  - Notionの競合キューURL（mybest等）をPuppeteer等でDOM解析し、**各商品にすでに貼られているアフィリエイトボタンの直リンク（楽天市場、Yahoo!ショッピング、Amazonへのリンク先URL）をそのまま直接スクレイピングして引っこ抜くこと。**
  - 外部の検索APIやJANコードによる「検索・逆引き」は、直接リンクがどうしても取得できなかった場合の最終手段（第二優先）とする。
* **アフィリエイトID・パラメータのクリーン化整形：**
  - **Yahoo!ショッピング (ValueCommerce MyLink)**:
    - 遷移先URL（`vc_url`）は、**`/product/` を含む製品比較ページを絶対に指定しない**こと（ValueCommerceのエラーになるため）。必ず、個別店舗の商品詳細ページまたは公式検索結果を指定すること。
    - **【超重要】`vc_url` のパラメータ値に含まれるスラッシュ（`/`）やコロン（`:`）なども含め、リダイレクト先URL全体を100%完全にURLエンコードすること**（エンコード漏れがあると、バリューコマース側で「広告配信終了」エラーになるため）。
    - 太郎さんのValueCommerceアフィリエイトID（`sid=3767611`, `pid=2201292`）を一律で適用すること。
  - **楽天市場 (楽天アフィリエイト)**:
    - 楽天アフィリエイトURL（`https://hb.afl.rakuten.co.jp/...`）から、モバイル用遷移パラメータ（`&m=...`）およびフリーズの原因となる `rafcid` パラメータはすべて削除・排除すること。
    - `pc=` パラメータのみの軽量で最も安定するURLに整形して使用すること。
    - 太郎さんの楽天アフィリエイトID（`52aa350c.c59bcb5a.52aa350d.c841a8ec`）を適用すること。

---

## 3. 執筆方針（編集方針ペルソナ）

執筆者は個人としての人格を持たず、「美容・スキンケア商品の比較記事を担当するリサーチ編集者」という編集方針に基づいて執筆する。

* **役割：**
  - 商品を使用した当事者としては語らない
  - 成分情報・メーカー公表情報・処方設計・価格・容量・ブランドの特徴などの公開情報を整理し、読者が比較・判断するための材料を提供する
  - 評価ではなく分析を行う

* **語ってよいもの：**
  - 成分・配合内容
  - 処方設計・技術的な特徴
  - 容量・価格
  - メーカー公表情報（香りの系統、対象肌質など）
  - 他製品との比較

* **語らないもの：**
  - 自分が使った感想・効果実感
  - 個人的な好み
  - 根拠のない推測
  - 「私が使ってみた」「実際に試した」「手に取ってみると」等の体験を装う表現

* **人間味の出し方：**
  - キャラクター性ではなく、「読者の状況への目配り」で温度を出す
  - 例：「ヒアルロン酸を配合しています」だけで終わらせず、「乾燥が気になる季節はもちろん、エアコン環境で過ごす時間が長い人にも注目されやすい成分です」のように、読者の生活文脈に接続する一文を添える

* **文体・口調：**
  - 語尾は「です」「ます」調を基本としつつ、すべての文を同じ語尾で終わらせない
  - 「かも」「てね」など柔らかい語尾や、強調したい箇所での「！」も適度に混ぜてよい（多用はしない）
  - 「〜だよ」「〜だね」「〜んだ」などの砕けた口語表現は禁止する
  - 「おすすめ」「ぴったり」「最適」「心強い」「頼もしい」「納得」「魅力」「味方」「パートナー」「相棒」などの評価表現は禁止する
  - 説明ばかりを続けない。事実説明・比較・読者の状況への接続・注意点・提案など、文の役割を段落の中でゆるやかに入れ替える
  - 文の長さを揃えない。短文・中文・長文を自然に混在させ、単調なリズムにしない
  - 詳しい文体・リズムのルールは ARTICLE_PROMPT.md の最新版を必ず参照すること。矛盾する場合は ARTICLE_PROMPT.md を優先する

---

## 4. Notion登録時のmy-best URL確認ルール (推測・適当入力の完全禁止)
競合キューにmy-bestのURLを登録する際は、URLの誤認やデッドリンクを防ぐため、以下のフローを絶対厳守すること。

* **URL推測の絶対禁止：**
  - URLのID（番号部分など）を推測・適当に入力することは絶対に禁止する。
* **検索と実在確認の徹底：**
  - 必ず事前にウェブ検索（カテゴリ名 + my-best など）を行い、実在する記事であることを確認すること。
* **ページタイトルの確認：**
  - 登録前に実際にそのURLにアクセス（HTTPリクエスト等）し、HTMLのページタイトルを取得・確認して、意図した美容記事カテゴリであるかダブルチェックすること。
* **「正しいはず」での報告禁止：**
  - 事前の実アクセス・確認を行わずに「正しいはず」という思い込みで報告・登録しないこと。違反した場合は作業やり直しとする。

---

## 5. 絶対に変更禁止なファイル（収益化・表示機能）
以下のファイルはたろさんの明示的な指示がない限り変更禁止：
- src/lib/api.ts
- src/components/RankingTable.tsx
記事本文・画像・アフィリエイトリンク生成は変更可。収益化機能と表示機能は触らない。
変更する場合は必ずたろさんに確認を取ること。

## 6. 記事生成後の確認ルール
- 通常の書き溜め時：
  - URL確認不要・そのまま次の記事へ進む
- 以下のファイルまたは処理を修正した時のみURL確認必須：
  - publish_article.py
  - prepare_stockpile.py
  - src/lib/api.ts
  - src/components/RankingTable.tsx
  - アフィリエイトリンク処理
  - 商品画像処理
  - アイキャッチ生成処理

- 確認必須時の手順：
  1. 修正・git push
  2. 公開URLをたろさんに報告（https://www.mikke-style.com/articles/[slug]）
  3. たろさんが実ページ確認
  4. OKが出たら終了（OKが出るまで次の記事に進まない）

## 7. アフィリエイトボタン・価格表示ルール
- 各ECモール（Amazon・楽天・Yahoo）のアフィリエイトボタンには価格を表示する
- 価格取得に成功した場合は取得価格を表示する
- オープン価格への自動フォールバックは禁止
- 価格取得失敗時は価格未取得として扱い、価格表示ロジックを削除してはならない
- ランキングタブ（おすすめ順・コスパ順・人気順）は価格データを元にソートが機能すること
- src/lib/api.tsとsrc/components/RankingTable.tsxの価格表示・ソートロジックは維持する

